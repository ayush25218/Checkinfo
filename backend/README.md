# Checkinfo Backend

This folder contains the shared data and controller helpers used by the API routes.
Replace the in-memory records in `checkinfo.ts` with database queries when production data is ready.
